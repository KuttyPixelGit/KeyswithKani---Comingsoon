# API Configuration (for development)
VITE_API_BASE_URL=/api  # Will be relative in production

# Production API URL (uncomment and update when deploying to production)
# VITE_API_BASE_URL=https://your-production-api-url.com/api

# Email Configuration (for production)
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password

# Google Gemini API Key (if used)
VITE_GEMINI_API_KEY=your-gemini-api-key
